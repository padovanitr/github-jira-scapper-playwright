{
  "name": "pr-playwright-scrapping",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "generate:github": "pnpm build && node dist/src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate:jira": "pnpm build && node dist/src/getJiraTickets"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.5.4",
    "dotenv": "^16.4.5",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "playwright": "^1.47.0"
  }
}
